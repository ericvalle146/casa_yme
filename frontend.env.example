# Variáveis de ambiente do front-end (Vite)
# URL do webhook do N8N (OBRIGATÓRIO)
VITE_WEBHOOK_URL=https://webhook.locusp.shop/webhook/mariana_imobiliaria

# Se o domínio acima não funcionar, tente:
# VITE_WEBHOOK_URL=https://webhook.locusup.shop/webhook/mariana_imobiliaria
# ou use o IP direto se necessário
