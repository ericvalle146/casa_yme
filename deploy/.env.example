# =========================================
# CONFIGURAÇÕES DE DOMÍNIO E SSL
# =========================================
# Domínios em produção
DOMAIN_FRONTEND=casayme.com.br
DOMAIN_BACKEND=backend.casayme.com.br

# E-mail para certificados SSL Let's Encrypt
LETSENCRYPT_EMAIL=contato@casayme.com.br

# =========================================
# CONFIGURAÇÕES DO FRONTEND
# =========================================
# URL base da API do backend (usado pelo frontend para fazer requisições)
VITE_API_BASE_URL=https://backend.casayme.com.br

# URL do webhook N8N para o formulário de contato
# Deixe vazio se não estiver usando webhook no momento
VITE_WEBHOOK_URL=

# =========================================
# CONFIGURAÇÕES DO BACKEND
# =========================================
PORT=4000
NODE_ENV=production

# CORS - permite o domínio do frontend acessar o backend
CORS_ORIGINS=https://casayme.com.br,http://casayme.com.br

# Webhook N8N (mesmo que VITE_WEBHOOK_URL, para uso no backend)
N8N_WEBHOOK_URL=

# =========================================
# CONFIGURAÇÕES DO BANCO DE DADOS POSTGRES
# =========================================
# Banco de dados externo já existente
DB_HOST=72.61.131.168
DB_PORT=5432
DB_USER=admin
DB_PASSWORD='a32js@31#t3?$1%&*!Sk45!'
DB_NAME=casa_yme

# URL completa do banco (gerada automaticamente)
DATABASE_URL='postgres://admin:a32js@31#t3?$1%&*!Sk45!@72.61.131.168:5432/casa_yme'

# =========================================
# CONFIGURAÇÕES DE AUTENTICAÇÃO JWT
# =========================================
# Chave secreta para os tokens JWT
# IMPORTANTE: O script de deploy vai gerar automaticamente uma chave segura
ACCESS_TOKEN_SECRET=MUDE_ISSO_PARA_UMA_STRING_BEM_SEGURA_E_ALEATORIA
ACCESS_TOKEN_TTL_MINUTES=15
REFRESH_TOKEN_TTL_DAYS=7
PASSWORD_SALT_ROUNDS=12

# =========================================
# CREDENCIAIS DO CONTAINER POSTGRES LOCAL
# =========================================
# Nota: Estas variáveis são para caso use o Postgres em container
# Como está usando banco externo, podem ser ignoradas
POSTGRES_USER=casayme
POSTGRES_PASSWORD=localdevpassword
POSTGRES_DB=casayme
